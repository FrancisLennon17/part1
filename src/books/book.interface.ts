export interface Book {
  title: string;
  isbn: string;
  author: string;
  subtitle: string;
  published: string; // for now...
  publisher: string;
  pages: number;
  description: string;
  website: string;
}
